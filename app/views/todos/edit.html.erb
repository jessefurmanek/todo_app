<h1>Edit Todo</h1>
  <%= form_for(@todo) do |t| %>
      <%= t.text_field :task, label: "Task" %>
      <%= t.date_field :due_date %>
      <%= t.check_box :complete %>
      <%= t.submit "Update Todo", class: "btn" %>
    <% end %>
</div>
